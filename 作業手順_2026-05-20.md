# 作業手順書（2026年5月20日）

NKP 上に Dify カタログアプリを登録し、Ollama を連携するまでに実施した手順の記録です。  
各コマンドに解説を付けています。

---

## 1. 環境・前提

| 項目 | 内容 |
|------|------|
| 作業日 | 2026-05-20 |
| NKP バージョン | v2.17.1（catalog v0.8.1） |
| 作業ディレクトリ | `<WORK_DIR>` |
| 利用クラスタ | **2つ**（後述のとおり用途が異なる） |

### クラスタの使い分け

| クラスタ名 | kubeconfig | API サーバー | 主な用途（本日） |
|------------|------------|--------------|------------------|
| `<PRIMARY_CLUSTER>` | `<PRIMARY_KUBECONFIG>`（デフォルト） | `<PRIMARY_API_SERVER>` | Ollama の初回デプロイ |
| `<SECONDARY_CLUSTER>` | `<SECONDARY_KUBECONFIG>` | `<SECONDARY_API_SERVER>` | Dify カタログアプリ、ワークスペース `<WORKSPACE_NAME>` |

> **注意:** `*.svc.cluster.local` は **同一クラスタ内** でのみ名前解決されます。Dify（`<SECONDARY_CLUSTER>`）から `ollama.ollama.svc.cluster.local` へ接続するには、Ollama も **`<SECONDARY_CLUSTER>`** 上にデプロイする必要があります。

### ワークスペースと namespace

`nkp get workspaces` の結果より:

| ワークスペース名（`--workspace` に指定） | Kubernetes namespace（`kubectl -n` に指定） |
|------------------------------------------|---------------------------------------------|
| `<WORKSPACE_NAME>` | `<WORKSPACE_NAMESPACE>` |

---

## 2. NKP CLI のインストール

### 2.1 バージョン確認（インストール後）

```bash
nkp version
```

**解説:** インストール済みの NKP および関連コンポーネント（catalog, kommander 等）のバージョンを表示します。

### 2.2 ダウンロード・インストール（実施済みの例）

```bash
curl -L -o nkp_v2.17.0_linux_amd64.tar.gz "<Nutanix ダウンロード URL>"
tar -zxvf nkp_v2.17.0_linux_amd64.tar.gz
sudo install -o root -g root -m 0755 nkp /usr/local/bin/nkp
```

**解説:** Nutanix 公式サイトから取得した tarball を展開し、`nkp` バイナリを `/usr/local/bin` に配置します。

---

## 3. クラスタ接続の設定

```bash
export KUBECONFIG=<SECONDARY_KUBECONFIG>
kubectl get node
```

**解説:** 以降の `kubectl` / `nkp`（カタログ登録など）は **`<SECONDARY_CLUSTER>`** を対象にするため、kubeconfig を明示的に指定します。エイリアス `k` を使っている場合も同様にこの環境変数が効きます。

---

## 4. Dify Helm チャートのパッケージ化と GHCR へのプッシュ

### 4.1 Git リポジトリの準備

```bash
git clone <GIT_REPO_URL>
cd <WORK_DIR>/<REPO_NAME>
```

**解説:** Dify 用 Helm チャート等を格納するリポジトリをクローンします。

### 4.2 Git ユーザー設定

```bash
git config --global user.name "<GIT_USER_NAME>"
git config --global user.email "<GIT_USER_EMAIL>"
```

**解説:** コミット時に記録される作者情報を設定します。

### 4.3 GitHub Container Registry へのログイン

```bash
export GHCR_USER=<GITHUB_USERNAME>
export GHCR_PAT=<GitHub Personal Access Token>
echo "$GHCR_PAT" | docker login ghcr.io -u "$GHCR_USER" --password-stdin
```

**解説:** Helm チャートを OCI として GHCR に push するため、Docker/Helm が使うレジストリ認証を行います。トークンは `read:packages` / `write:packages` 等、push に必要な権限を付与した PAT を使用します。

### 4.4 チャートのパッケージ化

```bash
cd <WORK_DIR>/<REPO_NAME>
helm package ./dify
```

**解説:** `Chart.yaml` を持つ `./dify` ディレクトリから `dify-0.1.0.tgz` のようなアーカイブを生成します。バージョンは `Chart.yaml` の `version` に依存します。

### 4.5 OCI レジストリへの push

```bash
export HELM_EXPERIMENTAL_OCI=1
helm push dify-0.1.0.tgz oci://ghcr.io/<GITHUB_USERNAME>/
```

**解説:** パッケージしたチャートを GHCR に OCI アーティファクトとしてアップロードします。成功時の例:

```
Pushed: ghcr.io/<GITHUB_USERNAME>/dify:0.1.0
```

> カタログの `OCIRepository` では **`oci://ghcr.io/<GITHUB_USERNAME>/dify`**（チャート名 `dify` まで含む）を指定する必要があります。`oci://ghcr.io/<GITHUB_USERNAME>/` のみだとバンドル作成時に `NAME_INVALID` になります。

---

## 5. NKP カタログリポジトリの生成

### 5.1 アプリケーションレイアウトの生成（1.11.5）

```bash
cd <WORK_DIR>
nkp generate catalog-repository --apps dify=1.11.5 --repo-dir ./
```

**解説:** カタログ用のディレクトリ構造（`applications/dify/1.11.5/` 以下の Kustomization、HelmRelease テンプレート等）をカレントディレクトリに生成します。`1.11.5` はカタログ上のアプリバージョンです。

### 5.2 別バージョンの生成（1.11.4）※任意

```bash
nkp generate catalog-repository --apps dify=1.11.4 --repo-dir ./
```

**解説:** 同じアプリの別バージョンを追加します。**2バージョン以上**ある状態でバンドル全体を作る場合は後述の `--collection-tag` が必要になります。

---

## 6. マニフェスト修正（OCIRepository URL）

**ファイル:** `applications/dify/1.11.5/helmrelease/helmrelease.yaml`（生成物）

**変更内容:**

```yaml
# 修正前（誤り）
url: oci://ghcr.io/<GITHUB_USERNAME>/

# 修正後（正）
url: oci://ghcr.io/<GITHUB_USERNAME>/dify
```

**解説:** `helm push` で登録されたリポジトリパス（`ghcr.io/<GITHUB_USERNAME>/dify`）と一致させます。タグ `0.1.0` は同ファイルの `spec.ref.tag` で指定します。

---

## 7. カタログバンドルの作成とプッシュ

### 7.1 バンドル作成（1バージョンのみ）

```bash
cd <WORK_DIR>
nkp create catalog-bundle --repo-dir . --apps dify=1.11.5
```

**解説:** 指定アプリのマニフェスト・イメージ参照を検証し、バンドルアーカイブ（例: `dify-1.11.5.tar`）を生成します。`--apps` で1つに絞れば `--collection-tag` は不要です。

**発生したエラー（修正前）:**

- `failed to pull oci repository "ghcr.io/<GITHUB_USERNAME>/:0.1.0"` → URL にチャート名がない
- 複数バージョンのみで実行した場合: `--collection-tag is required when more than 1 application is included in the bundle`

**複数バージョンをまとめる場合の例:**

```bash
nkp create catalog-bundle --repo-dir . --collection-tag dify-catalog
```

### 7.2 バンドルをレジストリへプッシュ

```bash
nkp push bundle --bundle <WORK_DIR>/dify-1.11.5.tar --to-registry oci://ghcr.io/<GITHUB_USERNAME>/
```

**解説:** バンドルに含まれるコンテナイメージ等を一時レジストリ経由で指定先 GHCR にアップロードします。完了後、カタログアプリ登録用の OCI URL が利用可能になります。

---

## 8. ワークスペースの確認

```bash
nkp get workspaces
```

**解説:** 利用可能なワークスペース一覧と、それぞれに対応する **Kubernetes namespace** を表示します。以降の `kubectl -n` には **NAMESPACE 列** の値を使います。

---

## 9. カタログアプリケーションの登録（Dify）

### 9.1 成功したコマンド

```bash
nkp create catalog-application \
  --url oci://ghcr.io/<GITHUB_USERNAME>/nutanix/dify \
  --tag 1.11.5 \
  --workspace <WORKSPACE_NAME> \
  --skip-oci-registry-patches
```

| オプション | 解説 |
|------------|------|
| `--url` | バンドル push 後の OCI リポジトリ URL。**タグは含めない** |
| `--tag` | アーティファクトのタグ（例: `1.11.5`） |
| `--workspace` | ワークスペース**名**（`nkp get workspaces` の NAME 列） |
| `--skip-oci-registry-patches` | NKP による OCIRepository への認証情報自動パッチをスキップ（手動でシークレット等を設定する場合） |

**成功時のメッセージ例:**

```
Catalog application <CATALOG_APP_NAME> created.
Use 'nkp edit ocirepository -n <WORKSPACE_NAMESPACE> <CATALOG_APP_NAME>' to change its configuration if needed.
```

### 9.2 失敗例と原因

| 誤った指定 | 問題 |
|----------|------|
| `--url .../dify:1.11.5` | タグを URL に含めるとリソース名に `:` が入り Kubernetes 名として不正 |
| 行継続 `\` の後に NBSP（ノンブレーキングスペース） | リソース名末尾に `\u00a0` が入り検証エラー |

> コマンドは **1行で実行**するか、`\` の直後は **改行のみ**（スペースを入れない）にしてください。

---

## 10. Docker Hub イメージプル用シークレットの作成

プライベートイメージ等を pull する場合、ワークスペース namespace に `docker-registry` 型 Secret を作成します。

```bash
kubectl create secret docker-registry dockerhub-cred \
  --docker-server=https://index.docker.io/v1/ \
  --docker-username=<Docker Hub ユーザー名> \
  --docker-password='<パスワード>' \
  -n <WORKSPACE_NAMESPACE>
```

**解説:**

- `docker-registry` 型は `kubernetes.io/dockerconfigjson` 形式で、Pod の `imagePullSecrets` から参照されます。
- `-n` にはワークスペース名ではなく **`<WORKSPACE_NAMESPACE>`**（実 namespace）を指定します。
- パスワードに `!` などシェル特殊文字がある場合は **シングルクォート**で囲みます。

**発生したエラー:**

| エラー | 原因 |
|--------|------|
| `exactly one NAME is required, got 5` | 行頭の NBSP により `--docker-server` 等がフラグとして認識されず、NAME として解釈された |
| `namespaces "<WORKSPACE_NAME>" not found` | ワークスペース名を namespace として指定した |

---

## 11. default ServiceAccount への imagePullSecrets 付与

```bash
kubectl patch serviceaccount default \
  -n <WORKSPACE_NAMESPACE> \
  -p '{"imagePullSecrets":[{"name":"dockerhub-cred"}]}'
```

**解説:** 同 namespace で `serviceAccountName` 未指定（default）の Pod が、作成した `dockerhub-cred` を使ってイメージを pull できるようにします。

**確認:**

```bash
kubectl get serviceaccount default -n <WORKSPACE_NAMESPACE> -o jsonpath='{.imagePullSecrets}'
```

期待値: `[{"name":"dockerhub-cred"}]`

> NBSP 混入時は `default` のパッチは成功しても、余分な `\u00a0` という名前の ServiceAccount で `NotFound` が出ることがあります。1行コマンドで再実行不要な場合もあります。

---

## 12. Ollama のデプロイ（Helm）

### 12.1 チャートの取得とインストール（`<PRIMARY_CLUSTER>` クラスタ・初回）

```bash
git clone <OLLAMA_HELM_REPO_URL>
cd <WORK_DIR>/<OLLAMA_HELM_DIR>
helm upgrade --install ollama . -n ollama --create-namespace --wait --timeout 15m
```

| オプション | 解説 |
|------------|------|
| `upgrade --install` | 未インストールなら install、済みなら upgrade |
| `-n ollama` | namespace `ollama` |
| `--create-namespace` | namespace が無ければ作成 |
| `--wait` | Pod が Ready になるまで待機 |
| `--timeout 15m` | 待機の上限（初回はイメージ約 4GB の pull で数分かかることがある） |

**解説:** この時点の `KUBECONFIG` がデフォルト（`<PRIMARY_CLUSTER>` クラスタ）の場合、Ollama は **`<PRIMARY_CLUSTER>` クラスタ**にデプロイされます。

### 12.2 デプロイ確認

```bash
helm list -n ollama
kubectl get pods -n ollama -o wide
kubectl get svc -n ollama -o wide
```

**解説:** Release 状態、Pod、Service（ClusterIP `11434`）を確認します。Service 名は `ollama` です。

### 12.3 モデルの pull

```bash
kubectl exec -it deploy/ollama -n ollama -- \
  ollama pull <OLLAMA_MODEL_NAME>
```

**解説:** Ollama Pod 内でモデルをダウンロードします。容量が大きいため時間がかかります。

```bash
kubectl exec deploy/ollama -n ollama -- ollama list
```

**解説:** 取り込み済みモデル一覧を表示します。

---

## 13. Dify から Ollama へ接続（トラブルシュート）

### 13.1 症状

Dify で Ollama モデルを登録・検証するとき、次のようなエラー:

```
Failed to resolve 'ollama.ollama.svc.cluster.local' ([Errno -2] Name or service not known)
```

### 13.2 原因

- **Dify** は `<SECONDARY_CLUSTER>` の `<WORKSPACE_NAMESPACE>` namespace で動作
- **Ollama** は初回、`<PRIMARY_CLUSTER>` クラスタ（別 API サーバー）にのみ存在
- クラスタ DNS（`*.svc.cluster.local`）はクラスタ外・別クラスタからは解決できない

### 13.3 推奨対処（`<SECONDARY_CLUSTER>` に Ollama を再デプロイ）

```bash
export KUBECONFIG=<SECONDARY_KUBECONFIG>
cd <WORK_DIR>/<OLLAMA_HELM_DIR>
helm upgrade --install ollama . -n ollama --create-namespace --wait --timeout 15m

kubectl exec -it deploy/ollama -n ollama -- \
  ollama pull <OLLAMA_MODEL_NAME>
```

**Dify のモデル設定例（同一クラスタ内）:**

| 項目 | 値 |
|------|-----|
| Base URL | `http://ollama.ollama.svc.cluster.local:11434` |
| モデル名 | `<OLLAMA_MODEL_NAME>` |

---

## 14. よくあるエラー一覧

| エラーメッセージ | 原因 | 対処 |
|------------------|------|------|
| `--collection-tag is required...` | カタログにアプリが2つ以上 | `--collection-tag` を付与、または `--apps` で1つに限定 |
| `ghcr.io/<GITHUB_USERNAME>/:0.1.0` NAME_INVALID | OCI URL にチャート名がない | `oci://ghcr.io/<GITHUB_USERNAME>/dify` に修正 |
| `<CATALOG_APP_NAME>:1.11.5\u00a0` is invalid | NBSP 混入 / URL にタグを含めた | 1行コマンド、`--url` と `--tag` を分離 |
| `exactly one NAME is required, got 5` | kubectl コマンドに NBSP | 手入力またはプレーンテキストで貼り付け |
| `namespaces "<WORKSPACE_NAME>" not found` | ワークスペース名を `-n` に指定 | `<WORKSPACE_NAMESPACE>` を使用 |
| `ollama.ollama.svc.cluster.local` 名前解決失敗 | Dify と Ollama が別クラスタ | `<SECONDARY_CLUSTER>` 側に Ollama をデプロイ |

---

## 15. 作業の流れ（サマリー）

```
[NKP CLI インストール]
    ↓
[Dify Helm チャート package → GHCR push]
    ↓
[nkp generate catalog-repository]
    ↓
[OCIRepository URL 修正]
    ↓
[nkp create catalog-bundle → nkp push bundle]
    ↓
[nkp create catalog-application（<SECONDARY_CLUSTER> / ワークスペース）]
    ↓
[docker-registry Secret + ServiceAccount patch]
    ↓
[Ollama Helm デプロイ → モデル pull]
    ↓
[Dify ↔ Ollama 連携] → 同一クラスタ（<SECONDARY_CLUSTER>）で Ollama を用意
```

---

## 16. セキュリティ上の注意

- GitHub PAT・Docker Hub パスワードはシェル履歴（`~/.bash_history`）に残る可能性があります。作業後は **トークン／パスワードのローテーション**を推奨します。
- 本ドキュメントでは認証情報を `<プレースホルダ>` で記載しています。実際の値は安全な場所（シークレット管理）に保管してください。

---

## 17. 参考コマンド（今後の運用）

```bash
# カタログ OCIRepository の編集
nkp edit ocirepository -n <WORKSPACE_NAMESPACE> <CATALOG_APP_NAME>

# Dify Pod 確認（<SECONDARY_CLUSTER>）
export KUBECONFIG=<SECONDARY_KUBECONFIG>
kubectl get pods -n <WORKSPACE_NAMESPACE>

# Ollama 確認（<SECONDARY_CLUSTER> にデプロイ後）
kubectl get pods,svc -n ollama
```

---

## 付録: プレースホルダ一覧

| プレースホルダ | 説明（例） |
|----------------|------------|
| `<WORK_DIR>` | 作業用ホームディレクトリ（例: `/home/<USER>`） |
| `<REPO_NAME>` | Dify 用リポジトリのディレクトリ名 |
| `<GIT_REPO_URL>` | Dify 用 Git リポジトリの clone URL |
| `<GIT_USER_NAME>` | Git コミット作者名 |
| `<GIT_USER_EMAIL>` | Git コミット用メールアドレス |
| `<GITHUB_USERNAME>` | GitHub / GHCR のユーザー名 |
| `<PRIMARY_CLUSTER>` | デフォルト kubeconfig が指すクラスタ名 |
| `<SECONDARY_CLUSTER>` | Dify 等をデプロイするクラスタ名 |
| `<PRIMARY_KUBECONFIG>` | デフォルト kubeconfig のパス（例: `~/.kube/config`） |
| `<SECONDARY_KUBECONFIG>` | セカンダリクラスタ用 kubeconfig の絶対パス |
| `<PRIMARY_API_SERVER>` | プライマリクラスタの API サーバー URL |
| `<SECONDARY_API_SERVER>` | セカンダリクラスタの API サーバー URL |
| `<WORKSPACE_NAME>` | `nkp get workspaces` の NAME 列 |
| `<WORKSPACE_NAMESPACE>` | 対応する Kubernetes namespace |
| `<CATALOG_APP_NAME>` | 登録されたカタログアプリ名（例: `nutanix-dify`） |
| `<OLLAMA_HELM_REPO_URL>` | Ollama Helm チャートの Git リポジトリ URL |
| `<OLLAMA_HELM_DIR>` | クローン先ディレクトリ名 |
| `<OLLAMA_MODEL_NAME>` | Ollama で利用するモデル名 |

---

*作成日: 2026-05-20*
